bulk_reference_creation <- function(path = getwd(),
                                    filename) {
  #check that file exists
  #check that file is a .csv

  #check that file has the appropraite columns

  #load file:
  upload_data <- read.csv(file=paste0(path, "/", filename))

}
